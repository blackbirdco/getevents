require 'spec_helper'

describe Getevents do
  it 'has a version number' do
    expect(Getevents::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
